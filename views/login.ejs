<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style.css" />
    <title>Document</title>
  </head>
  <body>
    <h1 style="color: crimson; text-align: center">
      Welcome from user login page !!!
    </h1>

    <% if(ex_user_name) { %>
    <h2 style="color: crimson; text-align: center"><%=ex_msg %></h2>
    <h2 style="color: crimson; text-align: center">
      Welcome Dear <%=ex_user_name %>
    </h2>

    <% }else if(user_new) { %>
    <h2 style="color: crimson; text-align: center"><%=msg_new %></h2>
    <h2 style="color: crimson; text-align: center">
      Welcome new User <%=user_new %>
    </h2>
    <% } else if(name_wrong_password) {%>
    <h2 style="color: crimson; text-align: center"><%=password_msg %></h2>
    <h2 style="color: crimson; text-align: center">
      <%=name_wrong_password %>
    </h2>
    <%} %>

    <form action="/login" method="post">
      <div class="col-sm-6">
        <label for="email" class="form-label">Email address</label>
        <input type="email" class="form-control" id="email" name="email" />
      </div>
      <div class="col-sm-6">
        <label for="password" class="form-label">Password</label>
        <input
          type="password"
          class="form-control"
          id="password"
          name="password"
        />
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </body>
</html>
